import re

def change(match):
    if match.group(0) == '&&':
        return 'and'
    else:
        return 'or'

n = int(input())

for _ in range(n):
    pattern = r'(?<= )(&&|\|\|)(?= )'
    print(re.sub(pattern, change, input()))
